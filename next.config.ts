import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "127.0.0.1", "lh3.googleusercontent.com", "rxkuxmrnjnpwlqtigpku.supabase.co"],
  }
};

export default nextConfig;
